const assert = require('assert');
const Plugin = require('../../lib/plugin.js');

describe('plugin.js', () => {
    it('getMountProperty', () => {
        class TestPlugin extends Plugin {
            
        }
    });

});
